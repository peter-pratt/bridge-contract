# Foundry
out/
cache/
broadcast/

# Dependencies (installed via `forge install`, not vendored)
lib/

# Env / secrets
.env
*.log

# local anvil integration run (devnet/01-deploy.sh)
devnet/anvil.log
devnet/anvil.pid
devnet/mint.env
devnet/rotate.env
devnet/mint2.env
.DS_Store
